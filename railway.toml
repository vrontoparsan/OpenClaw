[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/index.js gateway --bind 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
